<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Password Reset</title>
    <style>
      :root {
        --primary-color: #dc3545;
        --secondary-color: #c82333;
        --accent-color: #fd7e14;
        --text-color: #333;
        --light-text: #666;
        --background: #f8f9fa;
        --card-bg: #ffffff;
        --success-color: #28a745;
      }

      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        line-height: 1.6;
        color: var(--text-color);
        background-color: var(--background);
        margin: 0;
        padding: 0;
      }

      .email-container {
        max-width: 600px;
        margin: 20px auto;
        background-color: var(--card-bg);
        border-radius: 12px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        padding: 30px;
      }

      .header {
        text-align: center;
        margin-bottom: 30px;
      }

      .logo {
        width: 80px;
        height: 80px;
        background-color: var(--primary-color);
        border-radius: 50%;
        margin: 0 auto 15px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: bold;
        font-size: 24px;
      }

      .security-icon {
        width: 60px;
        height: 60px;
        background-color: var(--accent-color);
        border-radius: 50%;
        margin: 0 auto 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 24px;
      }

      h1 {
        color: var(--primary-color);
        font-size: 24px;
        margin-bottom: 20px;
        text-align: center;
      }

      h3 {
        color: var(--text-color);
        font-size: 20px;
        margin-bottom: 20px;
      }

      p {
        margin-bottom: 20px;
        color: var(--light-text);
        font-size: 16px;
      }

      .button-container {
        text-align: center;
        margin: 30px 0;
      }

      .reset-button {
        display: inline-block;
        padding: 15px 30px;
        font-size: 16px;
        font-weight: bold;
        color: #fff;
        background: linear-gradient(
          135deg,
          var(--primary-color),
          var(--secondary-color)
        );
        text-decoration: none;
        border-radius: 8px;
        box-shadow: 0 4px 10px rgba(220, 53, 69, 0.3);
        transition: all 0.3s ease;
      }

      .reset-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 15px rgba(220, 53, 69, 0.4);
      }

      .expiration-notice {
        background-color: #fff3cd;
        border: 1px solid #ffeaa7;
        border-radius: 6px;
        padding: 15px;
        margin: 20px 0;
        text-align: center;
      }

      .expiration-notice p {
        margin: 0;
        color: #856404;
        font-weight: 500;
      }

      .security-notice {
        background-color: #d1ecf1;
        border: 1px solid #bee5eb;
        border-radius: 6px;
        padding: 15px;
        margin: 20px 0;
      }

      .security-notice p {
        margin: 0;
        color: #0c5460;
        font-size: 14px;
      }

      .footer {
        margin-top: 40px;
        padding-top: 20px;
        border-top: 1px solid #eee;
        text-align: center;
        font-size: 14px;
        color: var(--light-text);
      }

      .signature {
        margin-top: 30px;
      }

      @media only screen and (max-width: 600px) {
        .email-container {
          width: 100%;
          border-radius: 0;
          padding: 20px;
        }

        .reset-button {
          padding: 12px 25px;
          font-size: 14px;
        }
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <div class="header">
        <div class="logo">LMS</div>
        <div class="security-icon">🔒</div>
      </div>

      <h1>Password Reset Request</h1>

      <h3>Dear <%= firstName %>,</h3>

      <p>
        We received a request to reset your password for your Learning
        Management System account.
      </p>

      <p>To reset your password, please click the button below:</p>

      <div class="button-container">
        <a
          href="http://localhost:3000/auth/reset-password?token=<%= token %>"
          class="reset-button"
        >
          Reset Password
        </a>
      </div>

      <div class="expiration-notice">
        <p>⏰ This reset link is valid for 3 minutes only</p>
      </div>

      <div class="security-notice">
        <p>
          <strong>Security Notice:</strong> If you did not request a password
          reset, please ignore this email. Your account remains secure and no
          changes have been made.
        </p>
      </div>

      <p>
        For your security, this link will expire automatically after the time
        limit. If you need to reset your password after expiration, please
        request a new reset link.
      </p>

      <div class="signature">
        <p>Best regards,<br /><strong>LMS Security Team</strong></p>
      </div>

      <div class="footer">
        <p>© 2024 Learning Management System. All rights reserved.</p>
        <p>This is an automated message, please do not reply to this email.</p>
      </div>
    </div>
  </body>
</html>
